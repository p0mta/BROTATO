package book4u;

import javax.swing.SwingUtilities;

public class main {

    public static void main(String[] args) {
        // Crear una instancia de la clase PantallaPrincipal
        Pantalla_principal pantalla = new Pantalla_principal();
        
        // Crear una instancia de la clase Login (suponiendo que esta es parte de tu aplicación)
        Login log = new Login();

        // Crear una instancia de la clase Historial
        Historial historial = new Historial();
    }
}

